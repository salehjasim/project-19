import { PrepareOptions, Extension, HookContext } from '../../types/extensions';
export declare class DefaultOutputPathExtension implements Extension {
    name: string;
    order: number;
    prepare(context: HookContext, options: PrepareOptions): Promise<PrepareOptions<import("../..").Codecs>>;
}
