import { RequestOptions, Extension, HookContext } from '../../types/extensions';
export declare class BaseResolverExtension implements Extension {
    name: string;
    order: number;
    request(context: HookContext, options: RequestOptions): RequestOptions;
}
