import { PrepareOptions, Extension, HookContext } from "../../types/extensions";
export declare class EnsureOutputDirectoryExtension implements Extension {
    name: string;
    order: number;
    prepare(_: HookContext, options: PrepareOptions): Promise<PrepareOptions<import("../..").Codecs>>;
}
