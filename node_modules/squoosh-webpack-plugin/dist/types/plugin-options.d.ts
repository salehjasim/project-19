import { SquooshEncodeOptions, Codecs } from './encoder-options';
import { Extension } from './extensions';
export declare type SquooshPluginOptions<Codec extends Codecs = Codecs> = {
    extensions: Array<Extension<Codec>> | ((defaultExtensions: Array<Extension>) => Array<Extension>);
    encoderOptions: SquooshEncodeOptions<Codec>;
    codec: Codec;
    useWorker: boolean;
    requestPrefix?: string;
    include?: RegExp | Array<RegExp>;
    exclude?: RegExp | Array<RegExp>;
    dirs?: Array<string>;
    outDir?: string;
    uuidNamespace: string;
    preserveFileName?: boolean;
};
