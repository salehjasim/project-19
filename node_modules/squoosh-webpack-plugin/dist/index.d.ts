import { Codecs, SquooshPluginOptions } from './types';
export * from './types';
export declare class SquooshPlugin<Codec extends Codecs = 'mozjpeg'> {
    private workerProcess;
    private options;
    constructor(options?: Partial<SquooshPluginOptions<Codec>>);
    private emitToWorker;
    apply(compiler: any): void;
    private webpack4Apply;
    private webpack5Apply;
    private handleBeforeCompile;
    private handleResolve;
    private handleStop;
    private validateOptions;
    private applyInitializeHooks;
    private applyRequestHooks;
    private applyPrepareHooks;
}
